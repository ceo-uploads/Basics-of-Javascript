let character = 'Hello';
let count = 8;
let rows = ["Naomi", "Quincy", "CamperChan"];

console.log(rows[0]);
rows[rows.length - 1 ] = 10;

// chapter 22 - we can assign the value of the array index with the referance of it's length...